# Windows MSI build machine — install alongside requirements.txt as needed.
# Canonical MSI: installer/windows/build_msi.py  |  Run: python setup_msi.py bdist_msi

cx_Freeze>=6.15
psutil>=5.9
pywin32>=306
pynput>=1.7
requests>=2.31
python-dotenv>=1.0
